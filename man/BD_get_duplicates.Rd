% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BD_get_duplicates.R
\name{BD_get_duplicates}
\alias{BD_get_duplicates}
\title{Get duplicate rows by one or more keys}
\usage{
BD_get_duplicates(
  data,
  ...,
  .min_n = 2,
  .keep_all = TRUE,
  na.rm = FALSE,
  .count_col = ".n",
  .sort = TRUE
)
}
\arguments{
\item{data}{A data.frame or tibble.}

\item{...}{Columns used as the duplicate keys (tidyselect supported).}

\item{.min_n}{Minimum frequency to keep (default 2).}

\item{.keep_all}{If TRUE, return all original rows with a count column.
If FALSE, return one row per key with the count only.}

\item{na.rm}{If TRUE, drop rows where any key column is NA before counting.}

\item{.count_col}{Name of the count column to add (default ".n").}

\item{.sort}{If TRUE, sort descending by count (then keys).}
}
\value{
A tibble with duplicates and a count column.
}
\description{
Find duplicates based on selected columns. If no columns are selected,
duplicates are computed across all columns.
}
\examples{
# One key
BD_get_duplicates(mtcars, cyl)
# Multiple keys
BD_get_duplicates(mtcars, cyl, gear, .keep_all = FALSE)
# All columns (exact duplicate rows)
BD_get_duplicates(mtcars)
}
