% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BD_1_cat.R
\name{BD_1_cat}
\alias{BD_1_cat}
\title{One-row n(\%) table for a categorical variable}
\usage{
BD_1_cat(
  dataset,
  var_name,
  display_name = var_name,
  digits = 1,
  include_missing = TRUE,
  missing_label = "Missing",
  sort_levels = c("desc", "asc", "alpha", "levels"),
  top_n = NULL,
  other_label = "Other"
)
}
\arguments{
\item{dataset}{A data frame containing \code{var_name}.}

\item{var_name}{String. The column to summarize.}

\item{display_name}{String. Label placed in the first column header.
Defaults to \code{var_name}.}

\item{digits}{Integer. Decimal places for percentages (default 1).}

\item{include_missing}{Logical. If \code{TRUE} (default), NA values are counted
under \code{missing_label}.}

\item{missing_label}{String used to label missing values (default "Missing").}

\item{sort_levels}{One of \code{"desc"} (default, by count desc),
\code{"asc"}, \code{"alpha"} (alphabetical), or \code{"levels"} (keep factor order).}

\item{top_n}{Integer or \code{NULL}. If set, keeps the \code{top_n} most frequent
levels and collapses the rest into an \code{"Other"} column (after sorting).}

\item{other_label}{String label used when collapsing to "Other" (default "Other").}
}
\value{
A one-row \code{tibble}: first column is \code{display_name},
then one column per level showing \code{"n (p\%)"}, ending with \code{Total}.
}
\description{
Builds a single, display-ready \code{tibble} row of counts and percentages
for each level of a categorical variable, plus a \code{Total} column.
}
\examples{
df <- data.frame(g = c("A","B","A", NA, "C","B","B"))
BD_1_cat(df, "g", display_name = "Group")
BD_1_cat(df, "g", digits = 0, sort_levels = "alpha", top_n = 2)

}
