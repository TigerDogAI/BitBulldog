% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BD_1_cont.R
\name{BD_1_cont}
\alias{BD_1_cont}
\title{One-line summary for a continuous variable}
\usage{
BD_1_cont(
  dataset,
  var_name,
  display_name = var_name,
  digits = 2,
  finite_only = TRUE,
  probs = c(0.01, 0.05, 0.1, 0.25, 0.5, 0.75, 0.9, 0.95, 0.99)
)
}
\arguments{
\item{dataset}{A data frame containing \code{var_name}.}

\item{var_name}{String. The column name in \code{dataset} to summarize (must be numeric).}

\item{display_name}{String. Label to show in the \code{Variable} column.
Defaults to \code{var_name}.}

\item{digits}{Integer. Number of decimal places for formatted outputs (default \code{2}).}

\item{finite_only}{Logical. If \code{TRUE} (default), exclude \code{Inf/-Inf} from summaries.}

\item{probs}{Numeric vector of quantile probabilities in the order you want reported.
Defaults to \code{c(0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)}.}
}
\value{
A one-row \code{tibble} with columns:
\itemize{
\item \code{Variable}, \code{Mean_SD}, \code{Median}, \code{Min_Max},
\item \code{P1}, \code{P5}, \code{P10}, \code{Q1}, \code{Q2}, \code{Q3}, \code{P90}, \code{P95}, \code{P99},
\item \code{Missing}, \code{N}, \code{NonMissing}, \code{MissingPct}.
}
}
\description{
Produces a single display-ready \code{tibble} row summarizing a numeric column:
mean (sd), median, min–max, selected percentiles, and missingness counts.
}
\details{
If all values are missing (or filtered out by \code{finite_only = TRUE}),
numeric summaries are returned as \code{NA_character_} while counts are provided.
}
\examples{
x <- data.frame(a = c(rnorm(100), NA, Inf))
BD_1_cont(x, "a", display_name = "My Var")

}
