% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BD_compare_balance.R
\name{BD_compare_balance}
\alias{BD_compare_balance}
\title{Compare covariate balance between two data frames}
\usage{
BD_compare_balance(
  df1,
  df2,
  vars,
  weights1 = NULL,
  weights2 = NULL,
  digits = 4,
  include_smd = FALSE
)
}
\arguments{
\item{df1, df2}{Data frames to compare.}

\item{vars}{Character vector of variable names to compare (can exist in either df).}

\item{weights1, weights2}{Optional numeric weight vectors aligned with \code{df1}/\code{df2}.}

\item{digits}{Integer; number of decimal places for numeric outputs (default 4).}

\item{include_smd}{Logical; if TRUE, include standardized mean difference (continuous only).}
}
\value{
A tibble with one row per variable (continuous) or per level (categorical).
}
\description{
For each variable in \code{vars}, reports (a) for continuous variables:
weighted/unweighted mean, SD, N, missingness, difference, and optional SMD;
(b) for categorical variables: per-level proportions in each data set,
N, missingness, and difference in proportions.
}
\examples{
set.seed(1)
a <- data.frame(x=rnorm(100), g=sample(c("M","F"),100,TRUE), z=runif(100))
b <- data.frame(x=rnorm(120, .2, 1.1), g=sample(c("M","F"),120,TRUE), z=runif(120))
w1 <- runif(100); w2 <- runif(120)
BD_compare_balance(a,b,vars=c("x","g","z"), weights1=w1, weights2=w2, include_smd=TRUE)

}
